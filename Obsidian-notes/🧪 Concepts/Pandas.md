# 🐼 Pandas

Библиотека Python для работы с табличными данными.

## Основные функции
- `pd.read_csv()` — загрузка
- `df.head()`, `df.info()`, `df.describe()`
- `df.isnull().sum()` — пропуски
- `df.drop()`, `df.fillna()` — очистка
- `df.groupby()` — агрегация

## Типы данных
- `int64`, `float64` — числа
- `object` — строки
- `bool` — логические

## Теги
#concept/pandas #python #dataframe