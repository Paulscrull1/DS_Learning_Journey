# 🧹 Data Cleaning

Процесс исправления или удаления некорректных, неполных, дублированных или форматирующих ошибки данных.

## Стратегии
- Удаление столбцов с >50% пропусков (`Cabin`)
- Заполнение пропусков:
  - Числовые: медиана, среднее
  - Категориальные: мода
- Группировка: `df.groupby(['Sex', 'Pclass'])['Age'].transform(lambda x: x.fillna(x.median()))`

## Теги
#concept/data-cleaning #missing-values #preprocessing