# 📚 Модуль 1: Работа с данными (Data Wrangling & EDA)

## Цель
Научиться загружать, очищать, анализировать и визуализировать данные.

## Мои выводы по Titanic

- Всего пассажиров: **891**.
- Пропущенные значения:
  - `Age`: 177 пропусков → будем заполнять медианой.
  - `Cabin`: 687 пропусков → скорее всего, удалим столбец.
  - `Embarked`: 2 пропуска → заполним модой ('S').
- Выживаемость:
  - Выжили: 342 (38.4%)
  - Не выжили: 549 (61.6%)
- Интересные признаки: `Sex`, `Pclass`, `Fare` — могут влиять на выживаемость.

## Ключевые этапы
1. **Загрузка**: `pd.read_csv()`
2. **Обзор**: `.head()`, `.info()`, `.describe()`
3. **Пропуски**: `df.isnull().sum()`
4. **Очистка**: удаление/заполнение пропусков, удаление дубликатов
5. **Визуализация**: гистограммы, boxplot, scatter, heatmap
6. **Интерпретация**: что говорят данные?

## Важные функции Pandas
- `df.shape`, `df.columns`
- `df.info()`, `df.describe()`
- `df.isnull().sum()`
- `df.dropna()`, `df.fillna()`
- `df.drop_duplicates()`
- `df.groupby()`, `df.value_counts()`

## Визуализация данных

### Основные типы графиков в EDA:
- **countplot** — распределение категорий (например, выживаемость).
- **histplot** — распределение числовых данных (например, возраст).
- **boxplot** — разброс и выбросы (пока не использовали).
- **barplot** — сравнение средних/сумм.

### Библиотеки:
- `matplotlib.pyplot` — базовая визуализация.
- `seaborn` — красивые, статистически ориентированные графики.

### Советы:
- Всегда добавляй **заголовок** и **подписи осей**.
- Используй `hue` для сравнения по группам (например, Survived).
- Размер графика: `plt.figure(figsize=(ширина, высота))`.
## Очистка данных (Data Cleaning)

### Стратегии обработки пропусков:
- **Удаление строк**: если пропусков мало и они случайны.
- **Удаление столбцов**: если пропусков > 50% или столбец бесполезен.
- **Заполнение**:
  - Числовые: медиана (устойчива к выбросам), среднее.
  - Категориальные: мода (самое частое значение).
  - Умное заполнение: группировка (например, по `Sex` и `Pclass`).

### Важно:
- Не используй среднее, если есть выбросы → используй **медиану**.
- Всегда проверяй результат: `df.isnull().sum()`.
## Теги
#module/1 #pandas #eda #data-cleaning